# Dev Log - 2026-05-12

## 2026-05-12 14:25 CST - 增加 Agent 交接快照能力

Agent: Codex
Status: done

### Summary
为 `dev-log` skill 增加临时 agent 交接快照能力，用于从 Codex 切换到 Claude Code、OpenCode 或其他 agent 前保存当前目标、进度、上下文和下一步。该能力定位为 `dev-log` 的独立子能力，不把 handoff 混入长期开发日志。

### Decisions
- 默认交接文件为 `.agents/handoff/current.md`，临时、可覆盖、默认不提交到 git。
- 交接快照由用户明确要求触发，普通任务完成仍写 dev log。
- 交接快照继续沿用严格脱敏规则，即使默认不提交也不记录敏感原文。

### Changes
- `SKILL.md`: 增加 handoff 触发、读取、写入和最终回复规则。
- `references/`: 补充交接快照的触发策略、读写流程、配置字段和脱敏要求。
- `templates/HANDOFF.md`: 新增交接快照模板。
- `examples/handoff-current.md`: 新增当前状态交接示例。
- `README.md`: 补充使用说明、包结构、配置示例和交接快照说明。
- `evals/evals.json`: 增加生成 handoff 和接手读取 handoff 的验收样例。

### Verification
- `jq . evals/evals.json >/dev/null`: passed
- 人工检查 README、SKILL、references 中 `.agents/handoff/current.md`、`handoff`、交接快照等术语一致。

### Next Steps
- 后续可在真实项目里用“我要切换到 Claude，生成 handoff”测试一次，看不同 agent 是否能稳定按快照接手。
